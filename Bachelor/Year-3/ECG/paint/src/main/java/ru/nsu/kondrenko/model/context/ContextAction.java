package ru.nsu.kondrenko.model.context;

public enum ContextAction {
    IDLE,
    REPAINT,
    OPEN_FILE,
    SAVE_FILE,
    EXIT,
    SELECT_COLOR,
    SELECT_THICKNESS,
    SELECT_NUMBER_OF_VERTICES,
    SELECT_RADIUS,
    SELECT_ROTATION,
    HANDLE_ERROR,
    DISPLAY_HELP,
    DISPLAY_ABOUT
}
